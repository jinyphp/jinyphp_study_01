---
layout: docs
title: JinyPHP 패키지 설치 및 설정 가이드
description: Laravel을 기반으로 한 JinyPHP 확장 프레임워크 설치 및 설정 방법
category: JinyPHP 패키지
permalink: /04.jinyphp.html
---

# JinyPHP 패키지 설치 및 설정 가이드

JinyPHP는 Laravel을 기반으로 구축된 확장 프레임워크로, ERP, CMS, 관리자 패널 등의 기능을 제공합니다.

## 1️⃣ JinyPHP 핵심 패키지 설치

### 1.1 JinyERP 핵심 패키지 설치

```bash
# JinyERP 코어 패키지들
composer require jiny/auth
composer require jiny/admin
composer require jiny/site

# 또는 로컬 패키지로 개발하는 경우
# composer.json에 추가
```

### 1.2 로컬 패키지 개발 설정

composer.json에 로컬 패키지 설정:
```json
{
    "repositories": [
        {
            "type": "path",
            "url": "./packages/jiny/*"
        }
    ],
    "require": {
        "jiny/auth": "dev-main",
        "jiny/admin": "dev-main",
        "jiny/site": "dev-main"
    }
}
```

**📁 JinyERP 패키지 관련 파일 경로:**
- **Composer 패키지**: `./vendor/jiny/`
- **로컬 패키지**: `./packages/jiny/`
- **Admin 패키지**: `./vendor/jiny/admin/`
- **Auth 패키지**: `./vendor/jiny/auth/`
- **Site 패키지**: `./vendor/jiny/site/`
- **패키지 설정**: `./config/jiny/`
- **발행된 에셋**: `./public/vendor/jiny/`

## 2️⃣ 추가 유용한 패키지

### 2.1 개발 및 확장 패키지

```bash
# 개발 도구
composer require --dev barryvdh/laravel-debugbar
composer require --dev barryvdh/laravel-ide-helper

# API 개발
composer require laravel/sanctum

# 파일 관리
composer require spatie/laravel-medialibrary

# 권한 관리
composer require spatie/laravel-permission

# 다국어 지원
composer require mcamara/laravel-localization

# 이미지 처리
composer require intervention/image

# Excel 처리
composer require maatwebsite/excel
```

### 2.2 패키지 설정 발행

```bash
# 인증 관련
php artisan vendor:publish --provider="Laravel\Sanctum\SanctumServiceProvider"

# Debugbar 설정
php artisan vendor:publish --provider="Barryvdh\Debugbar\ServiceProvider"

# 권한 관리
php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider"

# 미디어 라이브러리
php artisan vendor:publish --provider="Spatie\MediaLibrary\MediaLibraryServiceProvider" --tag="medialibrary-migrations"

# JinyPHP 관련 설정 발행 (예상)
php artisan vendor:publish --provider="Jiny\Auth\AuthServiceProvider"
php artisan vendor:publish --provider="Jiny\Admin\AdminServiceProvider"
php artisan vendor:publish --provider="Jiny\Site\SiteServiceProvider"
```

**📁 발행된 설정 파일 경로:**
- **Sanctum**: `./config/sanctum.php`
- **Debugbar**: `./config/debugbar.php`
- **Permission**: `./config/permission.php`
- **MediaLibrary**: `./config/media-library.php`
- **JinyPHP Auth**: `./config/jiny-auth.php`
- **JinyPHP Admin**: `./config/jiny-admin.php`
- **JinyPHP Site**: `./config/jiny-site.php`

## 3️⃣ JinyPHP 패키지 설정 및 초기화

### 3.1 패키지 마이그레이션 실행

```bash
# JinyPHP 관련 마이그레이션 실행
php artisan migrate

# 특정 패키지 마이그레이션만 실행하는 경우
php artisan migrate --path=/vendor/jiny/auth/database/migrations
php artisan migrate --path=/vendor/jiny/admin/database/migrations
php artisan migrate --path=/vendor/jiny/site/database/migrations
```

### 3.2 JinyPHP 시더 및 초기 데이터

```bash
# JinyPHP 관련 시더 실행 (있는 경우)
php artisan db:seed --class=JinyAuthSeeder
php artisan db:seed --class=JinyAdminSeeder
php artisan db:seed --class=JinySiteSeeder

# 전체 시더 실행
php artisan db:seed
```

### 3.3 JinyPHP 에셋 발행

```bash
# JinyPHP 정적 파일 발행
php artisan vendor:publish --tag=jiny-auth-assets
php artisan vendor:publish --tag=jiny-admin-assets
php artisan vendor:publish --tag=jiny-site-assets

# 또는 전체 에셋 발행
php artisan vendor:publish --tag=public
```

**📁 발행된 에셋 파일 경로:**
- **Auth 에셋**: `./public/vendor/jiny-auth/`
- **Admin 에셋**: `./public/vendor/jiny-admin/`
- **Site 에셋**: `./public/vendor/jiny-site/`
- **CSS 파일**: `./public/vendor/jiny-*/css/`
- **JS 파일**: `./public/vendor/jiny-*/js/`
- **이미지**: `./public/vendor/jiny-*/images/`

## 4️⃣ JinyPHP 환경 설정

### 4.1 .env 파일 JinyPHP 관련 설정

```env
# JinyPHP 기본 설정
JINY_AUTH_ENABLED=true
JINY_ADMIN_ENABLED=true
JINY_SITE_ENABLED=true

# JinyPHP Admin 설정
JINY_ADMIN_PREFIX=admin
JINY_ADMIN_MIDDLEWARE=web,auth
JINY_ADMIN_THEME=default

# JinyPHP Site 설정
JINY_SITE_PREFIX=site
JINY_SITE_THEME=default
JINY_SITE_CACHE_ENABLED=true

# JinyPHP Auth 설정
JINY_AUTH_GUARD=web
JINY_AUTH_PASSWORD_RESET=true
JINY_AUTH_EMAIL_VERIFICATION=false
```

### 4.2 config/app.php 서비스 프로바이더 등록

```php
'providers' => [
    // Laravel 기본 프로바이더들...

    // JinyPHP 서비스 프로바이더들
    Jiny\Auth\AuthServiceProvider::class,
    Jiny\Admin\AdminServiceProvider::class,
    Jiny\Site\SiteServiceProvider::class,
],
```

**📁 JinyPHP 설정 관련 파일 경로:**
- **App 설정**: `./config/app.php`
- **Auth 설정**: `./config/auth.php`
- **JinyPHP 설정들**: `./config/jiny-*.php`
- **라우트 캐시**: `./bootstrap/cache/routes-v7.php`
- **설정 캐시**: `./bootstrap/cache/config.php`

## 5️⃣ JinyPHP 개발 및 사용법

### 5.1 JinyPHP Admin 접속

```bash
# Admin 패널 접속 (기본 설정)
# http://localhost:8000/admin

# Admin 사용자 생성 (artisan 명령어가 있는 경우)
php artisan jiny:admin:create-user
```

### 5.2 JinyPHP 컴포넌트 생성

```bash
# JinyPHP 컴포넌트 생성 (예상 명령어)
php artisan make:jiny-component ComponentName
php artisan make:jiny-admin AdminComponentName
php artisan make:jiny-site SiteComponentName

# JinyPHP 모델 생성
php artisan make:jiny-model ModelName
```

### 5.3 JinyPHP 캐시 관리

```bash
# JinyPHP 관련 캐시 클리어
php artisan jiny:cache:clear

# JinyPHP 설정 캐시 재생성
php artisan jiny:config:cache

# 전체 최적화 (JinyPHP 포함)
php artisan optimize
php artisan jiny:optimize
```

**📁 JinyPHP 개발 관련 파일 경로:**
- **JinyPHP 컴포넌트**: `./app/Jiny/`
- **JinyPHP 뷰**: `./resources/views/jiny/`
- **JinyPHP 컨트롤러**: `./app/Http/Controllers/Jiny/`
- **JinyPHP 모델**: `./app/Models/Jiny/`
- **JinyPHP 라우트**: `./routes/jiny.php`
- **JinyPHP 캐시**: `./storage/framework/cache/jiny/`

---

## ✅ JinyPHP 설치 완료 체크리스트

### 1️⃣ 패키지 설치
- [ ] JinyERP 핵심 패키지 설치 (`jiny/auth`, `jiny/admin`, `jiny/site`)
- [ ] 추가 유용한 패키지 설치
- [ ] 로컬 패키지 설정 (개발 환경)
- [ ] 패키지 설정 파일 발행

### 2️⃣ 초기화 및 설정
- [ ] 마이그레이션 실행
- [ ] 시더 실행 (선택사항)
- [ ] 에셋 파일 발행
- [ ] .env 파일 JinyPHP 설정 추가
- [ ] 서비스 프로바이더 등록

### 3️⃣ 기능 확인
- [ ] JinyPHP Admin 패널 접속 확인
- [ ] JinyPHP 인증 시스템 동작 확인
- [ ] JinyPHP Site 기능 확인
- [ ] 각 패키지 에셋 로딩 확인

---

## 🔗 관련 문서

- [Laravel 개발 환경 설정](./01.Setup.md) - JinyPHP 설치 전 Laravel 기본 환경 구성
- [서버 배포 가이드](./Deploy.md) - JinyPHP 애플리케이션 서버 배포 방법

## 📚 추가 정보

### JinyPHP 공식 리소스
- **공식 문서**: [JinyPHP 문서](https://jinyphp.com/docs)
- **GitHub**: [JinyPHP GitHub](https://github.com/jinyphp)
- **커뮤니티**: [JinyPHP 커뮤니티](https://jinyphp.com/community)

### 개발 참고사항
- JinyPHP는 Laravel의 확장이므로 Laravel의 모든 기능을 그대로 사용할 수 있습니다
- JinyPHP 패키지들은 서로 독립적으로 동작하며 필요에 따라 선택적으로 설치 가능합니다
- 로컬 개발 시에는 패키지를 `./packages/jiny/` 디렉토리에서 직접 개발할 수 있습니다