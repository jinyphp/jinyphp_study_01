# Laravel 배포 가이드 - Ubuntu Server
클라우드에서 Ubuntu 서버를 생성하고 Laravel 프로젝트를 배포하는 과정을 단계별로 안내합니다.

## 최신 기술 스택 (Ubuntu 25.10)
- **PHP 8.4** - Property Hooks, JIT 최적화, 새로운 배열 함수
- **MySQL 8.4** - 향상된 성능과 보안
- **Nginx 1.28** - 최신 웹서버 기능
- **Node.js 20.x LTS** - 안정적인 프론트엔드 빌드
- **Redis 7.x** - 고성능 캐싱 및 세션 스토리지
- **Vultr 클라우드 서버 적용**

## 배포 단계 개요
라라벨 프로젝트를 배포하는 과정은 4단계 진행 됩니다.
- **📦 클라우드 준비** - Vultr 서버 생성 및 초기 접속
- **⚙️ 서버 설정** - PHP, MySQL, Redis, Nginx 등 기본 환경 구성
- **🚀 코드배포 + 도메인설정** - GitHub에서 Laravel 배포 및 HTTPS 도메인 연결
- **🔄 개발 업데이트** - 지속적 배포 및 유지보수 방법

---

## 1️⃣ 클라우드 준비

### 1.1 Vultr 인스턴스 생성

1. **Vultr 계정 로그인** 및 새 인스턴스 생성
2. **서버 설정**:
   - 서버 위치: Seoul, Korea (또는 Tokyo, Japan)
   - 서버 타입: Cloud Compute
   - 운영체제: Ubuntu 25.10 x64 (최신 버전)
   - 서버 플랜:
     - **테스트/학습용**: 1CPU, 1GB RAM (현재 가이드 기준)
     - **최소 권장**: 1CPU, 2GB RAM
     - **실용 권장**: 2CPU, 4GB RAM
   - 추가 기능: IPv6 활성화 (선택사항)
   - SSH Keys: SSH 키 추가 또는 비밀번호 설정

3. **배포 완료 대기** (약 1-2분)

> **⚠️ 주의사항:**
> - Ubuntu 25.10은 최신 버전으로 일부 클라우드 제공업체에서 아직 지원하지 않을 수 있습니다
> - Vultr에서 Ubuntu 25.10 이미지를 지원하는지 먼저 확인하세요
> - 안정성을 위해 프로덕션 환경에서는 Ubuntu 24.04 LTS 사용을 고려해보세요

---

### 📝 실제 운영 환경 최소 제한 사항

> **📌 Note**: 이 가이드는 테스트/학습 목적으로 1GB RAM 기준으로 작성되었습니다.
> 실제 운영 환경에서는 아래 제한 사항을 반드시 고려하세요.

#### 🏗️ **하드웨어 최소 제한 사항**

##### **소규모 서비스 (1-50 동시 사용자)**
- **CPU**: 2 vCPU (최소)
- **RAM**: 4GB (권장 8GB)
- **스토리지**: 40GB SSD
- **네트워크**: 2TB/월 트래픽

##### **중규모 서비스 (50-200 동시 사용자)**
- **CPU**: 4 vCPU
- **RAM**: 8GB (권장 16GB)
- **스토리지**: 80GB SSD + 백업
- **네트워크**: 5TB/월 트래픽
- **로드 밸런서**: 고려 필요

##### **대규모 서비스 (200+ 동시 사용자)**
- **CPU**: 8+ vCPU (멀티 서버)
- **RAM**: 16GB+ per server
- **스토리지**: 분산 스토리지 + CDN
- **데이터베이스**: 전용 DB 서버
- **캐시**: 전용 Redis 클러스터

#### 🔒 **보안 및 안정성 제한 사항**

```bash
# SSL 인증서 (필수)
- Let's Encrypt (무료) 또는 상용 SSL
- HTTPS 강제 리다이렉트
- HSTS 설정

# 백업 시스템 (필수)
- 데이터베이스 자동 백업 (일 1회 이상)
- 파일 시스템 백업
- 오프사이트 백업 저장

# 모니터링 (권장)
- 서버 리소스 모니터링
- 애플리케이션 성능 모니터링 (APM)
- 에러 로깅 및 알림 시스템
```

#### 📊 **성능 및 확장성 제한 사항**

##### **데이터베이스**
```sql
-- 운영 환경 MySQL 설정 예시
innodb_buffer_pool_size = 2G        -- RAM의 50-70%
max_connections = 100                -- 동시 접속 수
slow_query_log = ON                  -- 성능 모니터링
```

**📁 MySQL 설정 파일 경로 및 적용 방법:**
```bash
# 메인 설정 파일 편집
sudo nano /etc/mysql/mysql.conf.d/mysqld.cnf

# [mysqld] 섹션에 다음 설정 추가/수정:
[mysqld]
innodb_buffer_pool_size = 2G
max_connections = 100
slow_query_log = 1
slow_query_log_file = /var/log/mysql/mysql-slow.log
long_query_time = 2

# 설정 적용을 위한 MySQL 재시작
sudo systemctl restart mysql

# 설정 확인
mysql -u root -p -e "SHOW VARIABLES LIKE '%innodb_buffer_pool_size%';"
mysql -u root -p -e "SHOW VARIABLES LIKE '%max_connections%';"
```

**📂 관련 파일 경로:**
- **메인 설정**: `/etc/mysql/mysql.conf.d/mysqld.cnf`
- **클라이언트 설정**: `/etc/mysql/conf.d/mysql.cnf`
- **슬로우 쿼리 로그**: `/var/log/mysql/mysql-slow.log`
- **에러 로그**: `/var/log/mysql/error.log`
- **바이너리 로그**: `/var/log/mysql/mysql-bin.log`

##### **캐시 시스템**
```bash
# Redis 운영 설정
maxmemory 1gb                        -- 충분한 캐시 메모리
maxmemory-policy allkeys-lru         -- 메모리 정책
save 900 1                           -- 데이터 지속성
```

**📁 Redis 설정 파일 경로 및 적용 방법:**
```bash
# Redis 메인 설정 파일 편집
sudo nano /etc/redis/redis.conf

# 주요 성능 최적화 설정 추가/수정:
# 메모리 관리
maxmemory 1gb
maxmemory-policy allkeys-lru

# 데이터 지속성 설정
save 900 1                          # 900초에 1개 이상 변경시 저장
save 300 10                         # 300초에 10개 이상 변경시 저장
save 60 10000                       # 60초에 10000개 이상 변경시 저장

# 네트워크 최적화
timeout 300                         # 클라이언트 타임아웃
tcp-keepalive 300                   # TCP 연결 유지

# 로깅 설정
loglevel notice
logfile /var/log/redis/redis-server.log

# 보안 설정
requirepass your_strong_password_here
bind 127.0.0.1 ::1

# 설정 적용을 위한 Redis 재시작
sudo systemctl restart redis-server

# 설정 확인
redis-cli -a your_password CONFIG GET maxmemory
redis-cli -a your_password CONFIG GET maxmemory-policy
redis-cli -a your_password INFO memory
```

**📂 관련 파일 경로:**
- **메인 설정**: `/etc/redis/redis.conf`
- **백업 설정**: `/etc/redis/redis.conf.backup`
- **로그 파일**: `/var/log/redis/redis-server.log`
- **데이터 디렉토리**: `/var/lib/redis/`
- **RDB 스냅샷**: `/var/lib/redis/dump.rdb`
- **AOF 로그**: `/var/lib/redis/appendonly.aof`
- **PID 파일**: `/var/run/redis/redis-server.pid`
- **소켓 파일**: `/var/run/redis/redis-server.sock`

##### **웹서버 및 PHP**
```bash
# Nginx + PHP-FPM 운영 설정
worker_processes auto;               -- CPU 코어 수에 맞춤
pm.max_children = 50                 -- 동시 처리 요청 수
opcache.memory_consumption = 256     -- OPcache 메모리
```

**📁 Nginx 설정 파일 경로 및 적용 방법:**
```bash
# Nginx 메인 설정 파일 편집
sudo nano /etc/nginx/nginx.conf

# http 블록 내 주요 성능 최적화 설정:
http {
    # 워커 프로세스 최적화
    worker_processes auto;              # CPU 코어 수에 맞춤
    worker_connections 1024;            # 연결 수 제한

    # 성능 최적화
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;

    # Gzip 압축 활성화
    gzip on;
    gzip_vary on;
    gzip_min_length 1000;
    gzip_types text/plain text/css text/js application/javascript;

    # 클라이언트 요청 제한
    client_max_body_size 64M;
    client_body_buffer_size 1K;

    # 서버 토큰 숨기기
    server_tokens off;
}

# 설정 문법 확인
sudo nginx -t

# Nginx 재시작
sudo systemctl reload nginx
```

**📁 PHP-FPM 설정 파일 경로 및 적용 방법:**
```bash
# PHP-FPM 풀 설정 파일 편집
sudo nano /etc/php/8.4/fpm/pool.d/www.conf

# 주요 성능 최적화 설정:
[www]
# 프로세스 관리자 설정
pm = dynamic
pm.max_children = 50                 # 최대 자식 프로세스 수
pm.start_servers = 10               # 시작시 프로세스 수
pm.min_spare_servers = 5            # 최소 대기 프로세스
pm.max_spare_servers = 20           # 최대 대기 프로세스
pm.process_idle_timeout = 10s       # 유휴 프로세스 타임아웃
pm.max_requests = 500               # 프로세스당 최대 요청 수

# PHP.ini 설정 파일 편집
sudo nano /etc/php/8.4/fpm/php.ini

# OPcache 최적화 설정:
opcache.enable=1
opcache.memory_consumption=256       # OPcache 메모리
opcache.interned_strings_buffer=8
opcache.max_accelerated_files=10000
opcache.validate_timestamps=0        # 프로덕션에서는 0으로 설정
opcache.save_comments=0
opcache.fast_shutdown=1

# 일반 PHP 설정 최적화
memory_limit = 512M
post_max_size = 64M
upload_max_filesize = 64M
max_execution_time = 300
max_input_vars = 3000

# PHP-FPM 재시작
sudo systemctl restart php8.4-fpm

# 설정 확인
php -v
php -m | grep -i opcache
sudo systemctl status php8.4-fpm
```

**📂 관련 파일 경로:**

**Nginx:**
- **메인 설정**: `/etc/nginx/nginx.conf`
- **사이트 설정**: `/etc/nginx/sites-available/`
- **활성 사이트**: `/etc/nginx/sites-enabled/`
- **추가 설정**: `/etc/nginx/conf.d/`
- **에러 로그**: `/var/log/nginx/error.log`
- **액세스 로그**: `/var/log/nginx/access.log`

**PHP-FPM:**
- **메인 설정**: `/etc/php/8.4/fpm/php.ini`
- **풀 설정**: `/etc/php/8.4/fpm/pool.d/www.conf`
- **CLI 설정**: `/etc/php/8.4/cli/php.ini`
- **FPM 로그**: `/var/log/php8.4-fpm.log`
- **슬로우 로그**: `/var/log/php8.4-fpm.slow.log`
- **소켓 파일**: `/var/run/php/php8.4-fpm.sock`

#### 💰 **운영 비용 예상**

| 규모 | 월 서버 비용 | 추가 서비스 | 총 예상 비용 |
|------|-------------|-------------|--------------|
| **소규모** | $20-40 | CDN, 백업 | **$30-60/월** |
| **중규모** | $50-100 | 모니터링, 보안 | **$80-150/월** |
| **대규모** | $200+ | 로드밸런서, DB클러스터 | **$300+/월** |

#### 🔄 **확장성 고려사항**

##### **수직 확장 (Scale Up)**
```bash
# 서버 리소스 증가
1. CPU/RAM 업그레이드
2. 스토리지 확장
3. 네트워크 대역폭 증가
```

##### **수평 확장 (Scale Out)**
```bash
# 서버 수 증가
1. 로드 밸런서 도입
2. DB 마스터/슬레이브 구성
3. CDN 및 분산 캐시
4. 마이크로서비스 아키텍처
```

#### ⚡ **성능 벤치마크 기준**

```bash
# 운영 환경 성능 목표
- 평균 응답 시간: < 500ms
- 99% 응답 시간: < 2초
- 서버 가용률: > 99.9%
- 동시 접속자: 목표의 2-3배 여유
```

#### 📋 **운영 체크리스트**

- [ ] **모니터링**: Grafana + Prometheus 또는 상용 솔루션
- [ ] **로깅**: ELK Stack 또는 클라우드 로깅 서비스
- [ ] **백업**: 자동화된 백업 및 복구 테스트
- [ ] **보안**: 정기적인 보안 업데이트 및 취약점 스캔
- [ ] **성능**: 정기적인 성능 테스트 및 최적화
- [ ] **문서화**: 운영 매뉴얼 및 장애 대응 절차

---

### 1.2 서버 초기 접속 및 기본 설정

##### a) SSH 접속
```bash
# SSH로 서버 접속
ssh root@[서버_IP_주소]
```

##### b) 시스템 업데이트
```bash
# 시스템 패키지 업데이트
apt update && apt upgrade -y

# 시스템 재부팅 (커널 업데이트가 있는 경우)
# reboot
```

##### c) 필수 유틸리티 설치
```bash
# 기본 유틸리티 설치
apt install -y curl wget unzip software-properties-common apt-transport-https ca-certificates gnupg lsb-release

# 시스템 정보 확인
uname -a
lsb_release -a
```

### 1.3 클라우드 준비 완료 체크리스트

- [ ] Vultr 인스턴스 생성 완료
- [ ] SSH 접속 성공
- [ ] 시스템 업데이트 완료
- [ ] deploy 사용자 생성
- [ ] SSH 키 설정 (선택사항)
- [ ] 기본 방화벽 설정

---

## 2️⃣ 서버 설정

### 2.1 사용자 및 보안 설정

##### a) Deploy 사용자 생성
```bash
# 새 사용자 생성 (비밀번호 설정 포함)
adduser deploy

# sudo 그룹에 추가
usermod -aG sudo deploy
```

##### b) SSH 키 설정 (권장)
```bash
# SSH 디렉토리 생성
mkdir -p /home/deploy/.ssh

# 기존 SSH 키 복사 (root의 키를 복사)
cp ~/.ssh/authorized_keys /home/deploy/.ssh/

# 소유권 및 권한 설정
chown -R deploy:deploy /home/deploy/.ssh
chmod 700 /home/deploy/.ssh
chmod 600 /home/deploy/.ssh/authorized_keys
```

##### c) 보안 사용자로 전환
```bash
# deploy 사용자로 전환
su - deploy

# 현재 사용자 확인
whoami
pwd
```

### 2.2 방화벽 및 보안 설정

##### a) UFW 방화벽 기본 설정
```bash
# UFW 기본 정책 설정
sudo ufw default deny incoming
sudo ufw default allow outgoing

# SSH 포트 허용 (기본 22번 포트)
sudo ufw allow ssh
```

##### b) 웹서버 포트 허용
```bash
# HTTP 포트 (80) 허용
sudo ufw allow 80

# HTTPS 포트 (443) 허용
sudo ufw allow 443
```

##### c) 방화벽 활성화 및 확인
```bash
# UFW 방화벽 활성화
sudo ufw enable

# 방화벽 상태 확인
sudo ufw status verbose
```

### 2.3 PHP 8.4 설치 및 설정 (Ubuntu 25.10 기본)

##### a) 패키지 목록 업데이트
```bash
# 최신 패키지 정보 업데이트
sudo apt update
```

##### b) PHP 8.4 및 확장 모듈 설치
```bash
# PHP 8.4 코어 및 FPM 설치 (Ubuntu 25.10 기본 버전)
sudo apt install -y php8.4-fpm php8.4-cli

# Laravel 필수 확장 설치
sudo apt install -y php8.4-mysql php8.4-redis php8.4-xml php8.4-curl \
                   php8.4-zip php8.4-mbstring php8.4-bcmath php8.4-tokenizer \
                   php8.4-gd php8.4-intl php8.4-sqlite3

# 추가 유용한 확장
sudo apt install -y php8.4-soap php8.4-xmlrpc php8.4-imagick
```

##### c) PHP 8.4 새로운 기능 활용을 위한 설정
```bash
# PHP 8.4 최적화 설정
# 설정 파일 경로: /etc/php/8.4/fpm/php.ini
sudo sed -i 's/memory_limit = .*/memory_limit = 512M/' /etc/php/8.4/fpm/php.ini
sudo sed -i 's/upload_max_filesize = .*/upload_max_filesize = 64M/' /etc/php/8.4/fpm/php.ini
sudo sed -i 's/post_max_size = .*/post_max_size = 64M/' /etc/php/8.4/fpm/php.ini
sudo sed -i 's/max_execution_time = .*/max_execution_time = 300/' /etc/php/8.4/fpm/php.ini

# PHP 8.4 JIT 컴파일러 최적화 활성화
sudo sed -i 's/;opcache.enable=1/opcache.enable=1/' /etc/php/8.4/fpm/php.ini
sudo sed -i 's/;opcache.jit=1205/opcache.jit=1205/' /etc/php/8.4/fpm/php.ini
sudo sed -i 's/;opcache.jit_buffer_size=0/opcache.jit_buffer_size=64M/' /etc/php/8.4/fpm/php.ini

# 에러 로깅 설정 (프로덕션)
sudo sed -i 's/display_errors = .*/display_errors = Off/' /etc/php/8.4/fpm/php.ini
sudo sed -i 's/log_errors = .*/log_errors = On/' /etc/php/8.4/fpm/php.ini
```

**📁 주요 PHP 설정 파일 경로:**
- **PHP.ini**: `/etc/php/8.4/fpm/php.ini` (PHP-FPM용)
- **CLI용**: `/etc/php/8.4/cli/php.ini` (명령행용)
- **PHP-FPM 풀 설정**: `/etc/php/8.4/fpm/pool.d/www.conf`
- **로그 파일**: `/var/log/php8.4-fpm.log`

##### d) PHP-FPM 서비스 시작 및 확인
```bash
# PHP-FPM 서비스 시작
sudo systemctl start php8.4-fpm

# 부팅 시 자동 시작 설정
sudo systemctl enable php8.4-fpm

# 서비스 상태 확인
sudo systemctl status php8.4-fpm

# PHP 8.4 버전 및 새로운 기능 확인
php --version
php -m | grep -E "(JIT|opcache)"

# PHP 8.4 새로운 함수 테스트 (선택사항)
php -r "print_r(get_defined_functions()['internal']);" | grep -E "(array_find|array_any)"
```

> **💡 PHP 8.4의 새로운 기능들:**
> - **Property Hooks**: getter/setter를 더 간단하게 작성
> - **Asymmetric Visibility**: 속성의 읽기/쓰기 가시성 분리 설정
> - **새로운 배열 함수**: `array_find()`, `array_find_key()`, `array_any()`, `array_all()`
> - **JIT 최적화**: 향상된 성능
> - **보안 강화**: 더 안전한 난수 생성

### 2.4 Composer 설치

##### a) Composer 다운로드 및 설치
```bash
# Composer 인스톨러 다운로드 및 실행
curl -sS https://getcomposer.org/installer | php

# 시스템 전역 설치
sudo mv composer.phar /usr/local/bin/composer
sudo chmod +x /usr/local/bin/composer
```

##### b) 설치 확인
```bash
# Composer 버전 확인
composer --version

# 전역 사용 가능 확인
which composer
```

### 2.5 Git 설치 및 설정

##### a) Git 설치
```bash
# Git 패키지 설치
sudo apt install -y git
```

##### b) Git 전역 설정 (선택사항)
```bash
# 사용자 이름 설정
git config --global user.name "Your Name"

# 이메일 주소 설정
git config --global user.email "your.email@example.com"

# 기본 브랜치명 설정 (main으로 설정)
git config --global init.defaultBranch main
```

##### c) 설치 및 설정 확인
```bash
# Git 버전 확인
git --version

# Git 설정 확인
git config --list --global
```

### 2.6 MySQL 8.4 설치 및 설정 (Ubuntu 25.10 기본)

##### a) MySQL 8.4 서버 설치
```bash
# MySQL 8.4 서버 설치 (Ubuntu 25.10 기본 버전)
sudo apt install -y mysql-server

# MySQL 서비스 시작 및 자동 시작 설정
sudo systemctl start mysql
sudo systemctl enable mysql

# MySQL 8.4 버전 확인
mysql --version
```

##### b) MySQL 보안 설정
```bash
# MySQL 보안 설정 마법사 실행
sudo mysql_secure_installation
```

**보안 설정 과정에서 선택할 옵션:**
1. **VALIDATE PASSWORD 플러그인 설치**: `Y` (강력한 비밀번호 정책 적용)
2. **Root 비밀번호 설정**: `Y` → 강력한 비밀번호 입력
3. **익명 사용자 제거**: `Y`
4. **원격 root 로그인 금지**: `Y`
5. **테스트 데이터베이스 제거**: `Y`
6. **권한 테이블 다시 로드**: `Y`

##### c) 애플리케이션용 데이터베이스 생성
```bash
# MySQL 접속 (root 계정)
sudo mysql -u root -p
```

**MySQL 콘솔에서 실행:**
```sql
-- 애플리케이션용 데이터베이스 생성
CREATE DATABASE jinyerp_demo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 애플리케이션용 사용자 생성 (강력한 비밀번호 사용)
CREATE USER 'jinyerp'@'localhost' IDENTIFIED BY 'secure_password_here';

-- 데이터베이스 권한 부여
GRANT ALL PRIVILEGES ON jinyerp_demo.* TO 'jinyerp'@'localhost';

-- 권한 새로고침
FLUSH PRIVILEGES;

-- 생성된 데이터베이스 확인
SHOW DATABASES;

-- MySQL 종료
EXIT;
```

##### d) MySQL 설치 확인
```bash
# MySQL 서비스 상태 확인
sudo systemctl status mysql

# MySQL 연결 테스트
mysql -u jinyerp -p jinyerp_demo -e "SELECT 1;"
```

**📁 주요 MySQL 설정 파일 경로:**
- **메인 설정**: `/etc/mysql/mysql.conf.d/mysqld.cnf`
- **클라이언트 설정**: `/etc/mysql/conf.d/mysql.cnf`
- **데이터 디렉토리**: `/var/lib/mysql/`
- **로그 파일**: `/var/log/mysql/error.log`
- **소켓 파일**: `/var/run/mysqld/mysqld.sock`

### 2.7 Redis 7.x 설치 및 설정

##### a) Redis 7.x 서버 설치
```bash
# Redis 7.x 서버 설치 (Ubuntu 25.10 기본 버전)
sudo apt install -y redis-server

# Redis 서비스 시작
sudo systemctl start redis-server
sudo systemctl enable redis-server

# Redis 버전 확인
redis-server --version
```

##### b) Redis 보안 및 성능 설정
```bash
# Redis 설정 파일 백업
sudo cp /etc/redis/redis.conf /etc/redis/redis.conf.backup

# Redis 설정 편집
sudo nano /etc/redis/redis.conf
```

**Redis 설정 파일에서 수정할 내용:**
```
# 메모리 제한 설정 (서버 RAM의 절반 정도)
maxmemory 256mb
maxmemory-policy allkeys-lru

# 보안을 위한 비밀번호 설정
requirepass your_strong_redis_password_here

# 외부 접속 제한 (로컬에서만 접속 허용)
bind 127.0.0.1 ::1

# 보안 명령어 비활성화
rename-command FLUSHDB ""
rename-command FLUSHALL ""
```

##### c) Redis 재시작 및 확인
```bash
# Redis 설정 적용을 위한 재시작
sudo systemctl restart redis-server

# Redis 서비스 상태 확인
sudo systemctl status redis-server

# Redis 연결 테스트 (비밀번호 필요)
redis-cli -a your_strong_redis_password_here ping

# Redis 메모리 사용량 확인
redis-cli -a your_strong_redis_password_here info memory
```

**📁 주요 Redis 설정 파일 경로:**
- **메인 설정**: `/etc/redis/redis.conf`
- **백업 설정**: `/etc/redis/redis.conf.backup`
- **로그 파일**: `/var/log/redis/redis-server.log`
- **데이터 디렉토리**: `/var/lib/redis/`
- **PID 파일**: `/var/run/redis/redis-server.pid`

### 2.8 Nginx 1.28 웹서버 설치

##### a) Nginx 1.28 설치
```bash
# Nginx 1.28 패키지 설치 (Ubuntu 25.10 기본 버전)
sudo apt install -y nginx

# Nginx 서비스 시작 및 자동 시작 설정
sudo systemctl start nginx
sudo systemctl enable nginx

# Nginx 버전 확인
nginx -v
```

##### b) Nginx 기본 설정 확인
```bash
# Nginx 서비스 상태 확인
sudo systemctl status nginx

# 웹서버 동작 확인
curl http://localhost

# Nginx 설정 파일 문법 확인
sudo nginx -t
```

##### c) 기본 보안 설정
```bash
# Nginx 기본 사이트 비활성화 (선택사항)
sudo rm -f /etc/nginx/sites-enabled/default

# Nginx 메인 설정에서 서버 토큰 숨기기
sudo sed -i '/server_tokens/s/^#//' /etc/nginx/nginx.conf
sudo sed -i 's/server_tokens on/server_tokens off/' /etc/nginx/nginx.conf

# 설정 적용
sudo systemctl reload nginx
```

**📁 주요 Nginx 설정 파일 경로:**
- **메인 설정**: `/etc/nginx/nginx.conf`
- **사이트 설정 디렉토리**: `/etc/nginx/sites-available/`
- **활성화된 사이트**: `/etc/nginx/sites-enabled/`
- **추가 설정**: `/etc/nginx/conf.d/`
- **로그 디렉토리**: `/var/log/nginx/`
- **에러 로그**: `/var/log/nginx/error.log`
- **액세스 로그**: `/var/log/nginx/access.log`

### 2.9 Node.js 및 NPM 설치 (프론트엔드 빌드용)

##### a) Node.js 20.x LTS 저장소 추가
```bash
# NodeSource 저장소 스크립트 다운로드 및 실행
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -

# 패키지 목록 업데이트
sudo apt update
```

##### b) Node.js 및 NPM 설치
```bash
# Node.js 패키지 설치 (NPM 포함)
sudo apt install -y nodejs

# 필수 빌드 도구 설치
sudo apt install -y build-essential
```

##### c) 설치 확인 및 NPM 업데이트
```bash
# Node.js 및 NPM 버전 확인
node --version
npm --version

# NPM을 최신 버전으로 업데이트
sudo npm install -g npm@latest

# 전역 패키지 설치 권한 설정 (선택사항)
mkdir ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.profile
source ~/.profile
```

### 2.10 서버 설정 완료 확인

#### 서비스 상태 최종 확인
```bash
# 모든 설치된 서비스 상태 한 번에 확인
sudo systemctl status php8.4-fpm mysql redis-server nginx --no-pager

# 서비스 포트 확인
sudo netstat -tlnp | grep -E ':22|:80|:443|:3306|:6379'

# 메모리 사용량 확인
free -h
```

---

### ✅ 2️⃣ 서버 설정 완료 체크리스트

- [ ] Deploy 사용자 생성 및 보안 설정
- [ ] UFW 방화벽 설정 (SSH, HTTP, HTTPS 허용)
- [ ] PHP 8.4 설치 및 확장 모듈 설정 (JIT 최적화 포함)
- [ ] Composer 설치 및 전역 사용 가능 확인
- [ ] Git 설치 및 기본 설정
- [ ] MySQL 8.4 설치, 보안 설정, 데이터베이스 생성
- [ ] Redis 7.x 설치, 보안 설정, 성능 최적화
- [ ] Nginx 1.28 설치 및 기본 보안 설정
- [ ] Node.js 20.x LTS 및 NPM 설치
- [ ] 모든 서비스 정상 동작 확인

---

## 3️⃣ 코드배포 + 도메인설정

### 3.1 GitHub 저장소 준비 및 Laravel 배포

##### a) GitHub 저장소 확인 및 접근 설정
```bash
# GitHub 저장소 URL 확인 (예시)
# https://github.com/your-username/jinyerp_demo_01.git
# 또는 SSH: git@github.com:your-username/jinyerp_demo_01.git

# 공개 저장소인 경우 (HTTPS 클론)
# 별도 설정 불필요

# 비공개 저장소인 경우 - Personal Access Token 방법
# GitHub -> Settings -> Developer settings -> Personal access tokens

# 또는 SSH 키 방법 (권장)
ssh-keygen -t rsa -b 4096 -C "your.email@example.com"
cat ~/.ssh/id_rsa.pub  # 이 내용을 GitHub SSH Keys에 추가
```

##### b) 프로젝트 클론
```bash
# 웹 루트 디렉토리로 이동
cd /var/www

# 기존 디렉토리 확인 및 정리
ls -la

# GitHub에서 프로젝트 클론 (HTTPS 방식 - 공개 저장소)
sudo git clone https://github.com/your-username/jinyerp_demo_01.git

# 비공개 저장소인 경우 (Personal Access Token 사용)
# sudo git clone https://your_token@github.com/your-username/jinyerp_demo_01.git

# SSH 방식 (SSH 키 설정된 경우)
# sudo git clone git@github.com:your-username/jinyerp_demo_01.git
```

##### c) 소유권 및 권한 설정
```bash
# 웹서버 사용자로 소유권 변경
sudo chown -R www-data:www-data /var/www/jinyerp_demo_01

# 기본 권한 설정
sudo chmod -R 755 /var/www/jinyerp_demo_01

# Laravel 특수 디렉토리 권한 (나중에 다시 설정)
sudo chmod -R 775 /var/www/jinyerp_demo_01/storage
sudo chmod -R 775 /var/www/jinyerp_demo_01/bootstrap/cache

# 프로젝트 구조 확인
cd /var/www/jinyerp_demo_01
ls -la
```

### 3.2 PHP 의존성 및 Laravel 설정

##### a) Composer 의존성 설치
```bash
# 프로젝트 디렉토리로 이동
cd /var/www/jinyerp_demo_01

# composer.json 파일 확인
cat composer.json

# 프로덕션 환경용 의존성 설치 (테스트 환경이므로 dev 포함)
sudo -u www-data composer install --optimize-autoloader

# 1GB RAM 환경에서 메모리 부족 시
# sudo -u www-data composer install --optimize-autoloader --no-scripts
# sudo -u www-data composer run-script post-autoload-dump
```

##### b) 설치 확인
```bash
# Composer 의존성 확인
sudo -u www-data composer check-platform-reqs

# vendor 디렉토리 확인
ls -la vendor/

# Laravel 명령어 테스트
sudo -u www-data php artisan --version
```

##### c) 환경 설정 파일 구성

###### a) .env 파일 생성 및 기본 설정
```bash
# .env.example에서 복사
sudo -u www-data cp .env.example .env

# .env 파일 편집
sudo nano .env
```

###### b) .env 파일 상세 설정 (demo.jinyerp.com용)
```env
# 애플리케이션 기본 설정
APP_NAME="Jiny ERP Demo"
APP_ENV=production
APP_DEBUG=false
APP_KEY=
APP_TIMEZONE=Asia/Seoul
APP_URL=https://demo.jinyerp.com

# 데이터베이스 설정
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=jinyerp_demo
DB_USERNAME=jinyerp
DB_PASSWORD=secure_password_here

# 캐시 설정
CACHE_STORE=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120
QUEUE_CONNECTION=redis

# Redis 설정
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=your_redis_password_here
REDIS_PORT=6379
REDIS_DB=0

# 메일 설정
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@demo.jinyerp.com"
MAIL_FROM_NAME="${APP_NAME}"

# 로그 설정
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# 세션 설정
SESSION_DOMAIN=demo.jinyerp.com
SANCTUM_STATEFUL_DOMAINS=demo.jinyerp.com

# 파일 시스템
FILESYSTEM_DISK=local

# 브로드캐스트
BROADCAST_DRIVER=log

# 1GB RAM 최적화 설정
OCTANE_SERVER=swoole
SCOUT_DRIVER=null
```

###### c) .env 파일 보안 설정
```bash
# .env 파일 권한 제한 (중요!)
sudo chmod 600 /var/www/jinyerp_demo_01/.env
sudo chown www-data:www-data /var/www/jinyerp_demo_01/.env
```

**📁 주요 Laravel 설정 파일 경로:**
- **환경 설정**: `/var/www/jinyerp_demo_01/.env`
- **환경 예제**: `/var/www/jinyerp_demo_01/.env.example`
- **설정 디렉토리**: `/var/www/jinyerp_demo_01/config/`
- **앱 설정**: `/var/www/jinyerp_demo_01/config/app.php`
- **DB 설정**: `/var/www/jinyerp_demo_01/config/database.php`
- **캐시 설정**: `/var/www/jinyerp_demo_01/config/cache.php`
- **라우트 파일**: `/var/www/jinyerp_demo_01/routes/web.php`
- **로그 디렉토리**: `/var/www/jinyerp_demo_01/storage/logs/`
- **스토리지 디렉토리**: `/var/www/jinyerp_demo_01/storage/`

##### d) Laravel 애플리케이션 초기화

###### a) 애플리케이션 키 생성
```bash
# 고유한 애플리케이션 키 생성
sudo -u www-data php artisan key:generate

# .env 파일에서 APP_KEY 확인
grep APP_KEY /var/www/jinyerp_demo_01/.env
```

###### b) 데이터베이스 마이그레이션
```bash
# 데이터베이스 연결 테스트
sudo -u www-data php artisan migrate:status

# 마이그레이션 실행 (프로덕션 환경)
sudo -u www-data php artisan migrate --force

# 시더 실행 (초기 데이터가 있는 경우)
sudo -u www-data php artisan db:seed --force

# 또는 특정 시더 실행
# sudo -u www-data php artisan db:seed --class=UserSeeder --force
```

###### c) Laravel 저장소 및 캐시 설정
```bash
# storage 디렉토리를 public으로 링크 (이미지, 파일 업로드용)
sudo -u www-data php artisan storage:link

# 설정 캐시 생성 (성능 향상)
sudo -u www-data php artisan config:cache

# 라우트 캐시 생성
sudo -u www-data php artisan route:cache

# 뷰 캐시 생성
sudo -u www-data php artisan view:cache

# 이벤트 캐시 생성
sudo -u www-data php artisan event:cache

# 캐시 확인
ls -la bootstrap/cache/
```

###### d) 파일 권한 최종 설정
```bash
# Laravel 전체 디렉토리 소유권
sudo chown -R www-data:www-data /var/www/jinyerp_demo_01

# 일반 파일 권한
sudo find /var/www/jinyerp_demo_01-type f -exec chmod 644 {} \;

# 디렉토리 권한
sudo find /var/www/jinyerp_demo_01-type d -exec chmod 755 {} \;

# 쓰기 권한이 필요한 디렉토리
sudo chmod -R 775 /var/www/jinyerp_demo_01/storage
sudo chmod -R 775 /var/www/jinyerp_demo_01/bootstrap/cache

# 실행 권한이 필요한 파일
sudo chmod 755 /var/www/jinyerp_demo_01/artisan
```

##### e) 프론트엔드 빌드 (필요한 경우)

###### a) Node.js 의존성 설치
```bash
# package.json 확인
cat /var/www/jinyerp_demo_01/package.json

# Node.js 의존성 설치 (1GB RAM에서는 주의!)
cd /var/www/jinyerp_demo_01

# 메모리 제한 해제 후 설치
sudo -u www-data env NODE_OPTIONS="--max-old-space-size=512" npm ci

# 또는 yarn 사용 (설치된 경우)
# sudo -u www-data yarn install
```

###### b) 프론트엔드 빌드
```bash
# 프로덕션 빌드
sudo -u www-data npm run build

# 또는 개발용 빌드
# sudo -u www-data npm run dev

# 빌드 결과 확인
ls -la public/build/
```

##### f) Laravel 설정 검증

###### a) 애플리케이션 상태 확인
```bash
# Laravel 명령어 테스트
sudo -u www-data php artisan --version

# 설정 확인
sudo -u www-data php artisan config:show

# 환경 정보 확인
sudo -u www-data php artisan env

# 라우트 목록 확인
sudo -u www-data php artisan route:list
```

###### b) 데이터베이스 연결 테스트
```bash
# 데이터베이스 연결 확인
sudo -u www-data php artisan migrate:status

# Tinker로 데이터베이스 테스트
sudo -u www-data php artisan tinker
# DB::connection()->getPdo(); 입력
# exit 입력
```

###### c) 로그 확인
```bash
# Laravel 로그 디렉토리 확인
ls -la /var/www/jinyerp_demo_01/storage/logs/

# 현재 로그 확인
sudo tail -f /var/www/jinyerp_demo_01/storage/logs/laravel.log
```

---

#### ✅ Step 2 완료 체크리스트

- [ ] GitHub 저장소 정상 클론
- [ ] Composer 의존성 설치 완료
- [ ] .env 파일 설정 및 보안 적용
- [ ] 애플리케이션 키 생성
- [ ] 데이터베이스 마이그레이션 완료
- [ ] Laravel 캐시 설정
- [ ] 파일 권한 설정
- [ ] 프론트엔드 빌드 (필요시)
- [ ] 애플리케이션 상태 검증

### 3.3 Nginx 웹서버 및 HTTPS demo.jinyerp.com 설정

#### 3.3.1 도메인 DNS 설정 (demo.jinyerp.com)

##### a) 도메인 준비
```bash
# 서버의 공인 IP 주소 확인
curl ifconfig.me
# 또는
curl icanhazip.com

# 예시: 123.456.78.90
```

##### b) DNS 설정 방법

**방법 1: 도메인 업체의 DNS 관리 콘솔에서 설정**
```
- 도메인: jinyerp.com
- 레코드 타입: A 레코드
- 호스트명: demo (demo.jinyerp.com이 됨)
- 값: [서버 IP 주소] (예: 123.456.78.90)
- TTL: 300 초 (5분) 또는 기본값
```

**방법 2: CloudFlare 사용하는 경우**
```bash
# CloudFlare Dashboard에서
Type: A
Name: demo
IPv4 address: [서버 IP]
TTL: Auto
Proxy status: DNS only (주황색 구름 비활성화)
```

##### c) DNS 전파 확인
```bash
# DNS 설정 확인 (몇 분 후)
nslookup demo.jinyerp.com

# 또는 dig 명령어
dig demo.jinyerp.com A

# 결과 예시:
# demo.jinyerp.com.    300    IN    A    123.456.78.90
```

### 3.2 Nginx 가상 호스트 설정

#### a) 기본 HTTP 설정 파일 생성
```bash
# Nginx 사이트 설정 파일 생성
sudo nano /etc/nginx/sites-available/demo.jinyerp.com
```

#### b) 초기 HTTP 설정 (SSL 인증서 발급 전)
```nginx
# /etc/nginx/sites-available/demo.jinyerp.com
server {
    listen 80;
    listen [::]:80;

    server_name demo.jinyerp.com www.demo.jinyerp.com;
    root /var/www/jinyerp_demo_01/public;

    # Laravel 인덱스 파일 설정
    index index.php index.html index.htm;

    # 문자 인코딩
    charset utf-8;

    # Laravel 라우팅 설정
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP 파일 처리
    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/var/run/php/php8.4-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;

        # PHP-FPM 타임아웃 설정
        fastcgi_connect_timeout 300;
        fastcgi_send_timeout 300;
        fastcgi_read_timeout 300;
    }

    # 정적 파일 최적화
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Vary Accept-Encoding;
        access_log off;
    }

    # 숨김 파일 보호
    location ~ /\.(?!well-known).* {
        deny all;
        access_log off;
        log_not_found off;
    }

    # 일반적인 파일들 액세스 로그 제외
    location = /favicon.ico {
        access_log off;
        log_not_found off;
        expires 1y;
    }

    location = /robots.txt  {
        access_log off;
        log_not_found off;
    }

    # 보안 헤더
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # 오류 페이지를 Laravel로 전달
    error_page 404 /index.php;

    # 로그 설정
    access_log /var/log/nginx/demo.jinyerp.com.access.log;
    error_log /var/log/nginx/demo.jinyerp.com.error.log;
}
```

#### c) Nginx 설정 최적화 (1GB RAM용)
```bash
# Nginx 메인 설정 편집
sudo nano /etc/nginx/nginx.conf
```

주요 최적화 설정:
```nginx
# /etc/nginx/nginx.conf 내의 http 블록 안에 추가

# 작업자 프로세스 수 (CPU 코어 수에 맞춤)
worker_processes 1;  # 1GB RAM에서는 1개

# 이벤트 블록 설정
events {
    worker_connections 512;  # 1GB RAM용 제한
    use epoll;
}

http {
    # 기본 설정
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 15;  # 메모리 절약을 위해 낮춤
    types_hash_max_size 2048;

    # 서버 토큰 숨기기 (보안)
    server_tokens off;

    # Gzip 압축 설정
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1000;
    gzip_types
        application/atom+xml
        application/javascript
        application/json
        application/ld+json
        application/manifest+json
        application/rss+xml
        application/vnd.geo+json
        application/vnd.ms-fontobject
        application/x-font-ttf
        application/x-web-app-manifest+json
        application/xhtml+xml
        application/xml
        font/opentype
        image/bmp
        image/svg+xml
        image/x-icon
        text/cache-manifest
        text/css
        text/plain
        text/vcard
        text/vnd.rim.location.xloc
        text/vtt
        text/x-component
        text/x-cross-domain-policy;

    # 클라이언트 요청 제한 (메모리 절약)
    client_max_body_size 64M;  # 파일 업로드 제한
    client_body_buffer_size 1K;
    client_header_buffer_size 1k;
    large_client_header_buffers 2 1k;

    # 포함할 설정
    include /etc/nginx/mime.types;
    include /etc/nginx/conf.d/*.conf;
    include /etc/nginx/sites-enabled/*;
}
```

### 3.3 사이트 활성화 및 테스트

#### a) 사이트 활성화
```bash
# 심볼릭 링크 생성 (사이트 활성화)
sudo ln -s /etc/nginx/sites-available/demo.jinyerp.com /etc/nginx/sites-enabled/

# 기본 사이트 비활성화 (포트 충돌 방지)
sudo rm -f /etc/nginx/sites-enabled/default
```

#### b) Nginx 설정 검증
```bash
# Nginx 설정 문법 검사
sudo nginx -t

# 예상 출력:
# nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
# nginx: configuration file /etc/nginx/nginx.conf test is successful
```

#### c) Nginx 서비스 재시작
```bash
# Nginx 설정 다시 로드
sudo systemctl reload nginx

# 또는 재시작
# sudo systemctl restart nginx

# 서비스 상태 확인
sudo systemctl status nginx
```

#### d) HTTP 접속 테스트
```bash
# 로컬에서 테스트
curl -H "Host: demo.jinyerp.com" http://localhost

# 또는 실제 도메인으로 (DNS 전파 후)
curl -I http://demo.jinyerp.com
```

### 3.4 Let's Encrypt SSL 인증서 설치

#### a) Certbot 설치 확인
```bash
# Ubuntu 25.10에 Snapd 설치 확인
sudo systemctl status snapd

# Certbot 설치 (snap 방식)
sudo snap install core
sudo snap refresh core
sudo snap install --classic certbot

# Certbot 명령어 링크 생성
sudo ln -s /snap/bin/certbot /usr/bin/certbot

# 설치 확인
certbot --version
```

#### b) SSL 인증서 발급
```bash
# DNS가 전파된 후 SSL 인증서 발급
sudo certbot --nginx -d demo.jinyerp.com -d www.demo.jinyerp.com

# 실행 중 나오는 선택사항:
# 1. 이메일 주소 입력 (갱신 알림용)
# 2. 약관 동의: Y
# 3. 뉴스레터 수신: N (선택)
# 4. HTTP에서 HTTPS로 리다이렉트: 2 (Redirect 선택)
```

#### c) SSL 인증서 발급 확인
```bash
# 인증서 상태 확인
sudo certbot certificates

# SSL 설정 후 Nginx 설정 파일 확인
cat /etc/nginx/sites-available/demo.jinyerp.com
```

#### d) 자동 갱신 설정 및 테스트
```bash
# 자동 갱신 테스트 (실제 갱신 안함)
sudo certbot renew --dry-run

# 자동 갱신 크론잡 확인
sudo systemctl status snap.certbot.renew.timer

# 수동으로 크론잡 추가하려면
sudo crontab -e
# 다음 줄 추가: 0 2 * * * /usr/bin/certbot renew --quiet
```

### 3.5 HTTPS 설정 완료 후 최적화

#### a) SSL 설정 강화 (Nginx)
```bash
# SSL 설정 파일 생성
sudo nano /etc/nginx/conf.d/ssl.conf
```

SSL 최적화 설정:
```nginx
# /etc/nginx/conf.d/ssl.conf

# SSL 프로토콜 설정
ssl_protocols TLSv1.2 TLSv1.3;
ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
ssl_prefer_server_ciphers off;

# SSL 세션 설정
ssl_session_cache shared:SSL:10m;
ssl_session_timeout 10m;

# SSL 스테이플링
ssl_stapling on;
ssl_stapling_verify on;

# 보안 헤더
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
```

#### b) Laravel HTTPS 설정 업데이트
```bash
# .env 파일에서 HTTPS URL로 변경 확인
grep APP_URL /var/www/jinyerp_demo_01/.env

# 다음과 같이 되어있는지 확인:
# APP_URL=https://demo.jinyerp.com
```

#### c) Laravel 설정 캐시 재생성
```bash
# HTTPS 설정 반영을 위한 캐시 재생성
cd /var/www/jinyerp_demo_01
sudo -u www-data php artisan config:clear
sudo -u www-data php artisan config:cache
sudo -u www-data php artisan route:cache
```

### 3.6 방화벽 최종 설정

#### a) UFW 규칙 정리
```bash
# 현재 방화벽 상태 확인
sudo ufw status verbose

# Nginx Full 프로파일 허용 (HTTP + HTTPS)
sudo ufw allow 'Nginx Full'

# 기존 HTTP만 허용하는 규칙 제거 (있다면)
sudo ufw delete allow 'Nginx HTTP'

# 80번 포트만 허용하는 규칙 제거 (있다면)
sudo ufw delete allow 80

# 최종 방화벽 상태 확인
sudo ufw status numbered
```

### 3.7 종합 테스트 및 검증

#### a) 서비스 상태 확인
```bash
# 모든 관련 서비스 상태 확인
sudo systemctl status nginx php8.4-fpm mysql redis-server --no-pager

# 포트 사용 상황 확인
sudo netstat -tlnp | grep -E ':80|:443|:3306|:6379'
```

#### b) 웹사이트 접속 테스트
```bash
# HTTP → HTTPS 리다이렉트 확인
curl -I http://demo.jinyerp.com

# HTTPS 직접 접속 확인
curl -I https://demo.jinyerp.com

# SSL 인증서 정보 확인
openssl s_client -connect demo.jinyerp.com:443 -servername demo.jinyerp.com < /dev/null
```

#### c) Laravel 애플리케이션 확인
```bash
# Laravel 로그 실시간 확인
sudo tail -f /var/www/jinyerp_demo_01/storage/logs/laravel.log &

# Nginx 로그 실시간 확인
sudo tail -f /var/log/nginx/demo.jinyerp.com.access.log &
sudo tail -f /var/log/nginx/demo.jinyerp.com.error.log &

# 백그라운드 프로세스 종료
# kill %1 %2 %3 또는 Ctrl+C
```

#### d) 브라우저 테스트
1. **https://demo.jinyerp.com** 접속
2. **SSL 인증서 확인** (주소창의 자물쇠 아이콘)
3. **Laravel 기본 페이지 확인**
4. **HTTP → HTTPS 자동 리다이렉트 확인**
5. **기본 Laravel 라우트 테스트**

### 3.8 성능 및 보안 최적화

#### a) Laravel Mix/Vite 정적 파일 확인
```bash
# public 디렉토리 확인
ls -la /var/www/jinyerp_demo_01/public/

# CSS/JS 파일 접속 테스트
curl -I https://demo.jinyerp.com/css/app.css
curl -I https://demo.jinyerp.com/js/app.js
```

#### b) 캐시 및 압축 테스트
```bash
# Gzip 압축 확인
curl -H "Accept-Encoding: gzip" -I https://demo.jinyerp.com

# 정적 파일 캐시 헤더 확인
curl -I https://demo.jinyerp.com/favicon.ico
```

#### c) 보안 헤더 확인
```bash
# 보안 헤더 확인
curl -I https://demo.jinyerp.com | grep -E "Strict-Transport-Security|X-Frame-Options|X-Content-Type-Options"
```

---

## ✅ Step 3 완료 체크리스트

- [ ] **DNS 설정**: demo.jinyerp.com A 레코드 추가
- [ ] **DNS 전파 확인**: nslookup/dig로 확인
- [ ] **Nginx 설정**: 가상 호스트 설정 파일 작성
- [ ] **Nginx 활성화**: sites-enabled 링크 생성
- [ ] **HTTP 테스트**: 기본 접속 확인
- [ ] **SSL 인증서**: Let's Encrypt 인증서 발급
- [ ] **HTTPS 접속**: SSL 인증서 정상 동작 확인
- [ ] **리다이렉트 설정**: HTTP → HTTPS 자동 전환
- [ ] **Laravel 설정**: HTTPS URL 업데이트
- [ ] **방화벽 설정**: UFW 규칙 정리
- [ ] **성능 최적화**: Gzip, 캐시 헤더 설정
- [ ] **보안 강화**: 보안 헤더 적용
- [ ] **종합 테스트**: 브라우저 및 명령어 테스트

## 추가 설정 및 보안

### 크론잡 설정

```bash
# www-data 사용자의 크론탭 편집
sudo crontab -u www-data -e
```

크론잡 추가:
```
* * * * * cd /var/www/jinyerp_demo_01 && php artisan schedule:run >> /dev/null 2>&1
```

### PHP-FPM 최적화

```bash
# PHP-FPM 설정 편집 (경로: /etc/php/8.4/fpm/pool.d/www.conf)
sudo nano /etc/php/8.4/fpm/pool.d/www.conf
```

최적화 설정:
```ini
pm = dynamic
pm.max_children = 50
pm.start_servers = 5
pm.min_spare_servers = 5
pm.max_spare_servers = 35
pm.process_idle_timeout = 10s
```

```bash
# PHP-FPM 재시작
sudo systemctl restart php8.4-fpm
```

### 로그 로테이션 설정

```bash
# Laravel 로그 로테이션 설정 (경로: /etc/logrotate.d/laravel)
sudo nano /etc/logrotate.d/laravel
```

로그 로테이션 설정:
```
/var/www/jinyerp_demo_01/storage/logs/*.log {
    daily
    missingok
    rotate 14
    compress
    notifempty
    create 644 www-data www-data
}
```

**📁 시스템 로그 및 설정 파일 경로:**
- **시스템 로그**: `/var/log/syslog`
- **시스템 인증 로그**: `/var/log/auth.log`
- **크론잡 로그**: `/var/log/cron.log`
- **UFW 방화벽 로그**: `/var/log/ufw.log`
- **로그 로테이션 설정**: `/etc/logrotate.d/`
- **크론탭 설정**: `/etc/crontab`, `/var/spool/cron/crontabs/`
- **Supervisor 설정**: `/etc/supervisor/conf.d/`
- **SSL 인증서**: `/etc/letsencrypt/live/`

## 문제 해결

### 일반적인 문제들

1. **500 Internal Server Error**
   - Laravel 로그 확인: `/var/www/jinyerp_demo_01/storage/logs/laravel.log`
   - Nginx 오류 로그 확인: `/var/log/nginx/error.log`
   - 파일 권한 확인

2. **데이터베이스 연결 오류**
   - `.env` 파일의 DB 설정 확인
   - MySQL 서비스 상태 확인
   - 방화벽 설정 확인

3. **캐시 관련 문제**
   ```bash
   sudo -u www-data php artisan cache:clear
   sudo -u www-data php artisan config:clear
   sudo -u www-data php artisan route:clear
   sudo -u www-data php artisan view:clear
   ```

4. **권한 문제**
   ```bash
   sudo chown -R www-data:www-data /var/www/jinyerp_demo_01
   sudo chmod -R 755 /var/www/jinyerp_demo_01
   sudo chmod -R 775 /var/www/jinyerp_demo_01/storage /var/www/jinyerp_demo_01/bootstrap/cache
   ```

---

## ⚡ 1GB RAM 서버 최적화 (절약형 플랜용)

> **⚠️ 주의**: 1GB RAM은 매우 제한적입니다. 프로덕션 사용 시 최소 2GB 권장

### MySQL 최적화 (1GB RAM용)
```bash
# MySQL 설정 최적화 (경로: /etc/mysql/mysql.conf.d/mysqld.cnf)
sudo nano /etc/mysql/mysql.conf.d/mysqld.cnf
```

```ini
[mysqld]
# 메모리 사용량 최소화
innodb_buffer_pool_size = 128M
key_buffer_size = 32M
table_open_cache = 64
sort_buffer_size = 512K
read_buffer_size = 256K
read_rnd_buffer_size = 512K
max_connections = 10
thread_cache_size = 8
query_cache_size = 16M
tmp_table_size = 32M
max_heap_table_size = 32M
```

### Redis 최적화 (1GB RAM용)
```bash
# Redis 메모리 제한 강화 (경로: /etc/redis/redis.conf)
sudo sed -i 's/maxmemory 256mb/maxmemory 64mb/' /etc/redis/redis.conf
sudo sed -i 's/# maxmemory-policy noeviction/maxmemory-policy allkeys-lru/' /etc/redis/redis.conf
```

### PHP-FPM 최적화 (1GB RAM용)
```bash
# PHP-FPM 프로세스 수 최소화 (경로: /etc/php/8.4/fpm/pool.d/www.conf)
sudo nano /etc/php/8.4/fpm/pool.d/www.conf
```

```ini
[www]
pm = dynamic
pm.max_children = 8
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
pm.process_idle_timeout = 10s
pm.max_requests = 200
```

### 스왑 파일 생성 (필수)
```bash
# 2GB 스왑 파일 생성
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# 영구 설정
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

# 스왑 사용 빈도 조정
echo 'vm.swappiness=10' | sudo tee -a /etc/sysctl.conf
```

### Laravel 앱 최적화
```bash
# 캐시 최적화 (메모리 사용량 감소)
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan event:cache

# 불필요한 패키지 제거
composer install --no-dev --optimize-autoloader
```

### 모니터링 명령어
```bash
# 메모리 사용량 실시간 모니터링
watch -n 1 'free -h && echo "=== TOP 5 MEMORY USERS ===" && ps aux --sort=-%mem | head -6'

# 서비스별 메모리 사용량
sudo systemctl status mysql redis-server nginx php8.4-fpm --lines=0 | grep -E "Memory|Active"
```

### 1GB RAM 운영 팁
- **개발 도구 설치 금지** (vim만 사용)
- **로그 로테이션 필수** (디스크 공간 절약)
- **불필요한 서비스 비활성화**
- **정기적인 캐시 정리**

---

## 🎉 최신 기술 스택 활용 팁

### PHP 8.4 활용하기
```php
// Property Hooks 예시
class User {
    private string $_name;

    public string $name {
        get => $this->_name;
        set(string $value) {
            $this->_name = ucfirst($value);
        }
    }
}

// 새로운 배열 함수 활용
$users = [
    ['name' => 'John', 'active' => true],
    ['name' => 'Jane', 'active' => false],
];

$activeUser = array_find($users, fn($user) => $user['active']); // John
$hasActive = array_any($users, fn($user) => $user['active']);   // true
```

### MySQL 8.4 성능 최적화
```sql
-- 새로운 인덱스 힌트 활용
SELECT /*+ INDEX(users idx_name) */ * FROM users WHERE name = 'John';

-- JSON 함수 개선 활용
SELECT JSON_OVERLAPS('[1,2,3]', '[2,3,4]') as has_common; -- true
```

### 성능 모니터링
```bash
# PHP 8.4 JIT 상태 확인
php -r "var_dump(opcache_get_status()['jit']);"

# MySQL 8.4 성능 스키마 확인
mysql -e "SELECT * FROM performance_schema.events_statements_summary_by_digest LIMIT 10;"

# Redis 7.x 메모리 분석
redis-cli --latency-history -i 1
```

---

## 4️⃣ 개발 업데이트

### 4.1 지속적 배포 (CD) 설정

#### 자동 배포 스크립트
```bash
# 간단한 배포 스크립트 생성
sudo nano /home/deploy/deploy.sh
```

```bash
#!/bin/bash
# Laravel 자동 배포 스크립트

cd /var/www/jinyerp_demo_01

# Git 최신 코드 받기
sudo -u www-data git pull origin main

# 의존성 업데이트
sudo -u www-data composer install --optimize-autoloader --no-dev

# 캐시 갱신
sudo -u www-data php artisan config:cache
sudo -u www-data php artisan route:cache
sudo -u www-data php artisan view:cache

# 마이그레이션 실행
sudo -u www-data php artisan migrate --force

# 프론트엔드 빌드
sudo -u www-data npm ci && npm run build

echo "배포 완료!"
```

#### GitHub Webhooks 설정
```bash
# 웹훅 처리용 PHP 스크립트
sudo nano /var/www/jinyerp_demo_01/public/webhook.php
```

### 4.2 모니터링 및 로그 관리

#### 로그 모니터링
```bash
# Laravel 로그 실시간 확인
sudo tail -f /var/www/jinyerp_demo_01/storage/logs/laravel.log

# Nginx 로그 확인
sudo tail -f /var/log/nginx/demo.jinyerp.com.access.log
```

#### 자동 백업 설정
```bash
# 데이터베이스 백업 스크립트
sudo nano /home/deploy/backup.sh
```

### 4.3 성능 최적화

#### 캐시 최적화
```bash
# OpCache 상태 확인
sudo -u www-data php artisan optimize

# Redis 캐시 상태 확인
redis-cli info memory
```

---

## ✅ 4단계 배포 완료!

이 가이드를 따라 진행하면 **최신 Ubuntu 25.10**과 **PHP 8.4**의 강력한 기능을 활용한 Laravel 애플리케이션이 성공적으로 배포됩니다.

### 🎯 다음 단계: 개발 환경 설정
프로젝트 개발을 위한 초기 설정은 [`Setup.md`](./Setup.md) 문서를 참고하세요.